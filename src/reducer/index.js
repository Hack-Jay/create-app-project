
const reducer = {
    from(state = '北京', action) {
        const { type, payload } = action;
        switch(type) {
            default:
        }

        return state;
    }
}

export default reducer